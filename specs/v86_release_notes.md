# מלאכי V86 - תיקון הרשמה, טלפונים ודף ניהול

## מה תוקן
- תוקנה שגיאת הרשמה בצד לקוח: `Cannot read properties of null (reading 'reset')`.
- אחרי הרשמה מוצג קישור ניהול אישי כקישור לחיץ וברור, במקום טקסט בלבד.
- כפתור השליחה מושבת בזמן שליחה כדי לצמצם יצירת רשומות כפולות מלחיצה חוזרת.
- נוספה נורמליזציה של טלפונים ישראליים בצד השרת:
  - `0521234567` → `+972521234567`
  - `972521234567` → `+972521234567`
  - `+972521234567` נשאר תקין
- עודכנו placeholders בטופס כך שלא ייראה שחייבים להקליד דווקא `+972`.

## למה
בבדיקת live המשתמש קיבל WhatsApp, אבל באתר הופיעה שגיאת reset, נוצרו שתי רשומות, והניהול האישי לא נפתח בצורה ברורה. בנוסף היה בלבול סביב דרישת 972 במספרים.
