# מלאכי V18 - Release Notes

## נוסף
- tokenCreatedAt / tokenLastUsedAt / tokenRevokedAt למשפחות.
- endpoint יצירת קישור ניהול חדש: `/api/family/regenerate-token`.
- endpoint ביטול token: `/api/family/revoke-token`.
- כפתור בדשבורד ליצירת קישור ניהול חדש.
- תיעוד אירועי token ב-audit.

## בדיקות
- selftest עבר.
