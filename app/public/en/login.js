const API_BASE=(window.MALACHI_API_BASE||'').replace(/\/$/,''),form=document.querySelector('#loginForm'),result=document.querySelector('#loginResult');
async function api(path,options={}){const res=await fetch(API_BASE+path,{credentials:'include',headers:{'Content-Type':'application/json'},...options});const data=await res.json();if(!res.ok)throw new Error(data.error||'Something went wrong');return data}
form.addEventListener('submit',async event=>{event.preventDefault();const submit=form.querySelector('button[type=submit]'),payload=Object.fromEntries(new FormData(form).entries());submit.disabled=true;result.textContent='Signing in…';try{await api('/api/auth/login',{method:'POST',body:JSON.stringify(payload)});location.href='/en/dashboard.html'}catch(error){result.textContent='The email or password is incorrect.'}finally{submit.disabled=false}});
